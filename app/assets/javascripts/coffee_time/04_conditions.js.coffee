# Existence:
alert "Have coffee!" if sleepy?

# Id else as in Python (almost)
if happy and knowsIt
  clapsHands()
  chaChaCha()
else
  showIt()

# Why they don't like ? :
date = if friday then sue else jill

#because it means something else !!!!
but = 8 > 6 ? 'works' : 'right'

m = hasValue? 7

# some switch case... ah, when... then
score = 76
grade = switch
  when score < 60 then 'F'
  when score < 70 then 'D'
  when score < 80 then 'C'
  when score < 90 then 'B'
  else 'A'
  